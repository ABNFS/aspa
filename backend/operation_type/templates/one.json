{
    "id": {{ operation.id }},
    "name": "{{ operation.name }}",
    "alias": "{{ operation.alias}}"
    "accounts_type": [{% for a in operation.accounts_type %}{
"id": {{ a. id }},
"name": "{{ a.name}}",
"alias": "{{ a.alias }}",
"operation": {{ a.operation }}{% if loop.last %}}{% else %}},{% endif %}
{% endfor %}]
}